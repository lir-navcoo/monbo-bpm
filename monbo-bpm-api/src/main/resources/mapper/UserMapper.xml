<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.monbo.bpm.module.user.mapper.UserMapper">

    <!-- 精确查询用户名，用于登录认证等场景 -->
    <select id="findByUsername" resultType="com.monbo.bpm.module.user.entity.User">
        SELECT id, username, password, real_name, email, phone,
               status, deleted, created_time, updated_time
        FROM mb_user
        WHERE username = #{username}
          AND deleted = 0
    </select>

</mapper>
